# Generate by gcr.io/treactor/kpt-fn/gateway-api
apiVersion: apps/v1
kind: Deployment
metadata:
  name: envoy
  namespace: default
spec:
  template:
    spec:
      containers:
      - name: envoy
        image: envoyproxy/envoy-distroless:v1.23-latest
        env:
        - name: PORT
          value: "30000"
        ports:
        - name: http
          containerPort: 30000
          protocol: TCP
        - name: admin
          containerPort: 9901
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /ready
            port: admin
        volumeMounts:
        - name: config-volume
          mountPath: /etc/envoy
      volumes:
      - name: config-volume
        configMap:
          name: envoy
    metadata:
      labels:
        app: envoy
  selector:
    matchLabels:
      app: envoy
