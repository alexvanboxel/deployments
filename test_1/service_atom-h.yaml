# Generate by gcr.io/treactor/kpt-fn/create-atoms
apiVersion: v1
kind: Service
metadata:
  labels:
    app: atom-h
  name: atom-h
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 30000
  selector:
    app: atom-h
---
# Generate by gcr.io/treactor/kpt-fn/create-atoms
apiVersion: v1
kind: Service
metadata:
  labels:
    app: atom-h
  name: atom-h
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 30000
  selector:
    app: atom-h
---
# Generate by gcr.io/treactor/kpt-fn/create-atoms
apiVersion: v1
kind: Service
metadata:
  labels:
    app: atom-h
  name: atom-h
  namespace: default
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 30000
  selector:
    app: atom-h
